{
  "name": "portfolio_agent",
  "version": "1.0.0",
  "type": "module",
  "description": "A self-rebalancing portfolio manager that runs on Recall Network, maintains target allocations, and optionally uses OpenAI to tune weights over time.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.0"
  }
}
